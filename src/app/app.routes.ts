import { Routes } from '@angular/router';
import {SplineAnimationComponent} from "./components/spline-animation/spline-animation.component";

export const routes: Routes = [
  { path: '', component: SplineAnimationComponent },
];
